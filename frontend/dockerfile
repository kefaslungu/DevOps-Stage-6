FROM node:20

WORKDIR /app
COPY package*.json ./

# Install dependencies with legacy peer deps to avoid Webpack 2 conflicts
RUN npm install --legacy-peer-deps

COPY . .

# Expose port your dev server or build server uses
EXPOSE 8080

# Build frontend for production
CMD ["npm", "run", "build"]
